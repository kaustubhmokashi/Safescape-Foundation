name: Sync Instagram Posts

on:
  workflow_dispatch:
  schedule:
    - cron: '0 * * * *'

permissions:
  contents: write

jobs:
  sync-instagram:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Fetch Instagram posts and stories
        env:
          GRAPH_API_VERSION: v22.0
          IG_AUTH_USER_ID: ${{ secrets.IG_AUTH_USER_ID }}
          IG_ACCESS_TOKEN: ${{ secrets.IG_ACCESS_TOKEN }}
          TARGET_IG_USERNAME: ${{ secrets.TARGET_IG_USERNAME }}
          IG_PROFILE_URL: https://www.instagram.com/${{ secrets.TARGET_IG_USERNAME }}/
          IG_POSTS_LIMIT: '12'
        run: node scripts/fetch-instagram.mjs

      - name: Commit refreshed feed data
        run: |
          if git diff --quiet data/instagram-posts.json; then
            echo "No Instagram post changes to commit."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/instagram-posts.json
          git commit -m "chore: refresh instagram posts"
          git push
